# appFlavor

```dart
String? appFlavor
```

The flavor this app was built with.

This is equivalent to the value argued to the `--flavor` option at build time. This will be `null` if the `--flavor` option was not provided.
