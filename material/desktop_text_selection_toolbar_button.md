# DesktopTextSelectionToolbarButton

```dart
class DesktopTextSelectionToolbarButton extends StatelessWidget {}
```

A [TextButton] for the Material desktop text selection toolbar.

### DesktopTextSelectionToolbarButton()

```dart
DesktopTextSelectionToolbarButton({dynamic key, required VoidCallback? onPressed, required Widget child})
```

Creates an instance of DesktopTextSelectionToolbarButton.

### DesktopTextSelectionToolbarButton.text()

```dart
DesktopTextSelectionToolbarButton.text({dynamic key, required BuildContext context, required VoidCallback? onPressed, required String text})
```

Create an instance of [DesktopTextSelectionToolbarButton] whose child is a [Text] widget in the style of the Material text selection toolbar.

### onPressed

```dart
VoidCallback? onPressed
```

{@macro flutter.material.TextSelectionToolbarTextButton.onPressed}

### child

```dart
Widget child
```

{@macro flutter.material.TextSelectionToolbarTextButton.child}

### build()

```dart
Widget build(BuildContext context)
```
